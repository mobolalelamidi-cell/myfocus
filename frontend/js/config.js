// Keep this empty to use the browser's local storage on static hosts like Vercel.
// Add a hosted PHP API URL later if you deploy the backend separately.
// Example: window.MYFOCUS_API_BASE = "https://api.example.com/backend/api";
window.MYFOCUS_API_BASE = "";
